<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hyperdrive.woodstock">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.material.MaterialActivity"></activity>
        <activity
            android:name=".ui.operatingexpense.OperatingExpenseActivity"
            android:label="Despesas"
            android:parentActivityName=".MainActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.project.ProjectActivity"
            android:label="Projetos"
            android:parentActivityName=".ui.client.ClientActivity" />
        <activity
            android:name=".ui.cuttingplan.CuttingPlanActivity"
            android:label="Plano de Corte"
            android:parentActivityName=".ui.client.ClientActivity" />
        <activity
            android:name=".ui.budgetitem.BudgetItemActivity"
            android:label="Itens do Orçamento"
            android:parentActivityName=".ui.client.ClientActivity" />
        <activity
            android:name=".ui.budget.BudgetActivity"
            android:label="Orçamentos"
            android:parentActivityName=".ui.client.ClientActivity" />
        <activity
            android:name=".ui.client.ClientActivity"
            android:label="Clientes"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>