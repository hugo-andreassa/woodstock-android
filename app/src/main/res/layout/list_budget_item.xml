<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    card_view:cardCornerRadius="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:orientation="vertical">
        <TextView
            android:id="@+id/budgetitem_list_environment"
            android:textColor="#000"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <TextView
            android:id="@+id/budgetitem_list_description"
            android:textAlignment="inherit"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="6dp"
            android:layout_marginBottom="6dp"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end">

            <ImageButton
                android:id="@+id/budgetitem_list_more_button"
                android:src="@drawable/ic_add"
                android:background="?selectableItemBackground"
                style="@style/CustomImageButton"/>

            <ImageButton
                android:id="@+id/budgetitem_list_cut_button"
                android:src="@drawable/ic_cut_plan"
                android:background="?selectableItemBackground"
                style="@style/CustomImageButton"/>

            <ImageButton
                android:id="@+id/budgetitem_list_projects_button"
                android:src="@drawable/ic_projects"
                android:background="?selectableItemBackground"
                style="@style/CustomImageButton"/>
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>